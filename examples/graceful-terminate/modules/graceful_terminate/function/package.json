{
  "name": "gitlab-runner-graceful-stop",
  "version": "1.0.0",
  "description": "Gracefully stop the gitlab-runner service and check whether the service is stopped.",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-auto-scaling": "^3.112.0",
    "@aws-sdk/client-ec2": "^3.115.0",
    "@aws-sdk/client-ssm": "^3.112.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^1.0.0",
    "chai": "^4.3.6",
    "chai-as-promised": "^7.1.1",
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.2",
    "mocha-multi-reporters": "^1.5.1",
    "mocha-suppress-logs": "^0.3.1",
    "mochawesome": "^7.1.3",
    "sinon": "^14.0.0",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  },
  "scripts": {
    "clean": "rm -rf ./node_modules ./dist *.zip",
    "build": "webpack",
    "test": "mocha --require mocha-suppress-logs --reporter mocha-multi-reporters --reporter-options configFile=test/reporter-config.json"
  },
  "engines": {
    "node": "16.*",
    "npm": "please-use-yarn",
    "yarn": "1.*"
  }
}
